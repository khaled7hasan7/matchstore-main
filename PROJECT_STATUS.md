# تقرير حالة المشروع — MatchStore (مكتبة ابن تيمية)

**تاريخ الفحص:** 2026-08-27
**الخلاصة:** المشروع **غير جاهز للإطلاق حالياً**. البنية الأساسية قوية ومكتملة (كتالوج، سلة، حسابات، لوحات تحكم)، لكن توجد **ثغرات أمنية حرجة**، ومسار الدفع الإلكتروني غير مكتمل (الدفع عند الاستلام فقط يعمل)، ولا توجد رسائل بريد للطلبات، ولا يوجد أي إعداد للنشر. الانتقال إلى Supabase **ممكن وجهده معقول** (التفاصيل في القسم 5).

**تقدير الجهد الإجمالي للوصول للإطلاق: 2–3 أسابيع عمل مركّز.**

---

## 1. نظرة عامة على المشروع

| البند | التفاصيل |
|---|---|
| النوع | منصة تجارة إلكترونية متعددة البائعين (Multi-Vendor) — نسخة معدّلة من مشروع Velstore مفتوح المصدر |
| الهوية الحالية | يجري تحويله إلى **مكتبة ابن تيمية** (متجر كتب عربي، عملة JOD، شحن للأردن وفلسطين) عبر `IbnTaymiyyahBookstoreSeeder` |
| التقنيات | Laravel 10 · PHP 8.1+ · MariaDB 10.11 (حالياً) · Bootstrap 5 · Vite 5 |
| الحجم | 55 جدول · 47 موديل · 69 متحكم (Controller) · ~200 مسار (Route) · 124 ملف Blade |
| اللغات | عربي + إنجليزي — الترجمة العربية مكتملة ~99.5% مع دعم RTL كامل |
| اللوحات | أدمن (`/admin`) · بائع (`/seller`) · متجر العملاء (`/`) + واجهة API للقراءة |
| بوابات الدفع | الدفع عند الاستلام (يعمل) · PayPal (شبه مكتمل) · Stripe (تجريبي غير صالح) |
| سجل Git | **commit واحد فقط** (`v1`) — لا يوجد تاريخ تطوير أو نقطة رجوع |

---

## 2. ما هو جاهز ويعمل ✅

- **إدارة الكتالوج كاملة:** منتجات + متغيرات (variants) + خصائص (attributes) + صور + تصنيفات + علامات تجارية + بانرات + قوائم + صفحات CMS + بطاقات ترويجية — كلها بنظام ترجمة متعدد اللغات.
- **لوحة البائع:** إدارة منتجات وطلبات ومراجعات وملف شخصي.
- **تجربة التسوق:** سلة (Session) + كوبونات + مفضلة (Wishlist) + بحث + فلاتر + مراجعات + عرض سريع.
- **حسابات العملاء:** تسجيل/دخول/استعادة كلمة مرور + عناوين متعددة + سجل طلبات (3 أنظمة دخول منفصلة: أدمن/بائع/عميل).
- **الدفع عند الاستلام (COD):** المسار الوحيد المكتمل من السلة حتى تأكيد الطلب.
- **بيانات الشحن:** محافظات الأردن وفلسطين مع التكاليف (Seeder جاهز).
- **الواجهة:** ثيم xylo متجاوب Mobile-First، وملفات الإنتاج مبنية (`public/build` موجود).
- **نقاط قوة في الكود:** لا يوجد أي موديل بـ `$guarded = []` (حماية Mass Assignment سليمة) · حماية CSRF بدون استثناءات · كلمات المرور مشفرة bcrypt · لا توجد مفاتيح API مسربة في الكود.

---

## 3. المشاكل الحرجة — يجب إصلاحها قبل أي نشر 🔴

### 3.1 ثغرات أمنية

| # | الثغرة | الموقع | الأثر |
|---|---|---|---|
| 1 | **مسارات إعدادات الموقع بدون حماية** — أربعة مسارات `site-settings` (قراءة وتعديل ورفع ملفات وتغيير الثيم) خارج مجموعة `auth` | `routes/web.php:185-188` | أي زائر مجهول يستطيع تغيير اسم الموقع والشعار والثيم ورفع ملفات — تشويه كامل للموقع |
| 2 | **رفع SVG بدون حماية → XSS مخزّن** — الشعار والأيقونة يقبلان SVG (يمكن أن يحمل `<script>`) عبر المسار غير المحمي أعلاه | `SiteSettingsController.php:40-41` و `BrandController.php:44,76` | تنفيذ سكربت على كل زائر بما فيهم الأدمن |
| 3 | **لا يوجد نظام صلاحيات** — لوحة الأدمن محمية بـ `auth` فقط؛ جدول `users` بلا عمود دور/صلاحية، ولا توجد Policies | `routes/web.php:55` · `app/Models/User.php` | أي حساب في جدول users = أدمن كامل (طلبات، عملاء، مفاتيح بوابات الدفع) |
| 4 | **مراجعات بدون تسجيل دخول ومعتمدة تلقائياً** (`is_approved => 1`) | `routes/store.php:56` · `Store/ReviewController.php:27-33` | سبام مراجعات غير محدود على أي منتج |
| 5 | **XSS عبر وصف المنتج** — `{!! $product->translation->description !!}` يعرض HTML البائع بدون تعقيم، ولا يوجد HTML Purifier في المشروع | `themes/xylo/product-detail.blade.php:1893,2221` | بائع خبيث يحقن سكربت في صفحات المنتجات |
| 6 | **لا يوجد حد لمحاولات الدخول** (Throttling) لدخول العميل والبائع | `Store/Auth/LoginController.php` · `Vendor/Auth/AuthController.php` | هجمات تخمين كلمات المرور |
| 7 | **Session Fixation** — دخول البائع لا يستدعي `session()->regenerate()` | `Vendor/Auth/AuthController.php:25-27` | اختطاف جلسات |
| 8 | **حالة البائع غير مفعّلة** — البائع الموقوف/المحظور يدخل لوحته بشكل طبيعي | `Middleware/AuthenticateVendor.php` | لا يمكن إيقاف بائع فعلياً |
| 9 | **بائع يحذف طلبات بائعين آخرين** — يكفي أن يملك منتجاً واحداً في الطلب ليحذف الطلب كاملاً | `Vendor/OrderController.php:59-64` | تدمير سجلات بائعين آخرين وسجل العميل |
| 10 | **مفتاح تشفير حقيقي منشور** في `.env.example` (سطر 3) | `.env.example:3` | كل من ينسخه يحصل على مفتاح معروف → تزوير الجلسات. يجب تفريغه وتوليد مفتاح جديد للإنتاج |
| 11 | **نسختا قاعدة بيانات مرفوعتان في المستودع** (`backup.sql` و`backup_clean.sql`) | جذر المشروع | قد تحويان بيانات شخصية/هاشات — يجب حذفهما من Git |
| 12 | **حسابات افتراضية ضعيفة** — المثبّت ينشئ `admin@example.com` / `abc123` | `Console/Commands/MatchStore.php:88-92` | حساب أدمن معروف للجميع إن شُغّل المثبّت على الإنتاج |
| 13 | **تسجيل Debug يفضح البيانات** — `Log::info('All Request Data:', $request->all())` يكتب كامل الطلبات في السجل | `SiteSettingsController.php:102-189` | تسريب بيانات + تضخم سجلات |

### 3.2 وظائف معطوبة أو ناقصة

| # | المشكلة | الموقع | التفاصيل |
|---|---|---|---|
| 1 | **مسار `thankyou` غير موجود** — `checkout.store` يعيد التوجيه لمسار غير معرّف → استثناء | `CheckoutController.php:225` | يوجد مساران متكرران للـ checkout (`store.php:47-48` و `web.php:190-191`) والأقدم منهما معطوب — يجب حذف المسار القديم |
| 2 | **Stripe غير صالح للاستخدام** — مبلغ ثابت 1000 سنت (10$) بغضّ النظر عن السلة، بيئة sandbox ثابتة، لا Webhook، لا يُسجَّل طلب أو دفعة | `Store/PaymentGateway/StripeController.php:14-17` · `StripeService.php` | إعادة بناء كاملة مطلوبة (PaymentIntent بالمبلغ الفعلي + Webhook + تسجيل Payment/Order) |
| 3 | **PayPal بلا وضع Live** — `'sandbox'` ثابتة في 3 مواضع، ولا تحقق من صحة `?token` عند الرجوع | `CheckoutController.php:116,142` · `StripeController.php:13` | يعمل تجريبياً فقط |
| 4 | **خطأ جلب مفاتيح البوابات** — `getConfigValue($key, $default)` يستقبل البيئة كوسيط ثانٍ لكنه لا يفلتر عليها؛ مع وجود مفاتيح sandbox وlive معاً قد تُستخدم **مفاتيح Live وهو يظن أنه sandbox** | `Models/PaymentGateway.php:24` | خطير مالياً |
| 5 | **خطأ تشفير إعدادات البوابات** — القيمة تُشفَّر عند الحفظ ولا تُفكّ عند القراءة → تُرسَل مفاتيح مشفرة (خربانة) لـ Stripe/PayPal | `Models/PaymentGatewayConfig.php:17-20` | عملياً يجبر التخزين بنص صريح |
| 6 | **الشحن نصف معطّل** — مسارات إدارة مناطق الشحن معلّقة بتعليق، وصفحة الدفع تثبّت `$shipping = null` | `routes/web.php:150-153` · `CheckoutController.php:43` | لا تُحتسب تكلفة شحن رغم جاهزية الجداول والبيانات |
| 7 | **لا يوجد أي بريد للطلبات** — لا تأكيد طلب، لا إشعار بائع، لا إيصال دفع، لا رد على نموذج التواصل (يوجد Mailable واحد فقط للنشرة البريدية) | `app/Mail/` | أساسي لأي متجر |
| 8 | **الاستردادات (Refunds) للعرض فقط** — لا يوجد إجراء موافقة/تنفيذ | `Admin/RefundController.php` | |
| 9 | **API معطوب** — `apiResource('brands')` مسجّل لكن المتحكم يحوي `index` فقط → المسارات الأخرى ترجع 500 | `routes/api.php:39` | |
| 10 | **خطر بيع فوق المخزون** — خصم المخزون داخل Transaction لكن بدون `lockForUpdate()` — يزداد سوءاً على PostgreSQL | `CheckoutController` | |
| 11 | **البائع لا يسجّل نفسه** — لا صفحة تسجيل بائع؛ الأدمن ينشئ البائعين فقط (ولا يمكنه تعديلهم — لا يوجد edit/update) | `Admin/VendorController.php` | قرار منتج: هل هذا مقصود؟ |
| 12 | **لا توجد صور منتجات على القرص** — مجلد `storage/app/public/products` غير موجود أصلاً | `storage/app/public/` | البيانات التجريبية بلا صور |

### 3.3 مشاكل بيئة النشر

| # | المشكلة | الأثر |
|---|---|---|
| 1 | **ملف `public/hot` موجود** — يجعل Laravel يطلب الأصول من خادم التطوير `[::1]:5173` بدل `public/build` | الموقع سيظهر **بلا تصميم ولا جافاسكربت** على أي خادم — احذفه قبل النشر |
| 2 | **رابط `public/storage` معطوب** — يشير لمسار XAMPP القديم `C:\xampp\htdocs\...` | كل الصور المرفوعة لن تظهر — أعد تنفيذ `php artisan storage:link` |
| 3 | **`.env` بإعدادات تطوير بالكامل** — `APP_DEBUG=true` (يفضح كل شيء عند أي خطأ)، `QUEUE_CONNECTION=sync`، بريد `mailpit`، قاعدة بيانات `root` بلا كلمة مرور | يجب ملف `.env` إنتاجي كامل |
| 4 | **صفر اختبارات فعلية** — ملفا Laravel الافتراضيان فقط | لا شبكة أمان لأي تعديل |
| 5 | **لا CI ولا Docker ولا سكربت نشر** — النشر يدوي بالكامل | مع النقطتين 1 و2 أعلاه، النشر اليدوي سيفشل غالباً |
| 6 | **الاعتماد على قاعدة البيانات في كل طلب** — `LocaleMiddleware` يستعلم `site_settings` في كل صفحة؛ إن سقطت القاعدة يسقط الموقع كاملاً بخطأ 500 | أضف معالجة فشل (fallback) |

---

## 4. قائمة التحقق قبل الإطلاق (مرتبة بالأولوية)

### حرج — لا نشر قبلها
- [ ] نقل مسارات `site-settings` داخل مجموعة `auth` (§3.1-1)
- [ ] إزالة `svg` من قوائم `mimes:` في كل نقاط الرفع (§3.1-2)
- [ ] إضافة عمود دور للمستخدمين + Middleware `admin` لكل لوحة الأدمن (§3.1-3)
- [ ] حماية المراجعات بـ `auth.customer` وإيقاف الاعتماد التلقائي (§3.1-4)
- [ ] تعقيم وصف المنتج (HTMLPurifier) أو عرضه مهرّباً (§3.1-5)
- [ ] حذف `public/hot` وإعادة إنشاء `storage` symlink (§3.3-1,2)
- [ ] `.env` إنتاجي: `APP_DEBUG=false` · `APP_ENV=production` · مفتاح جديد `php artisan key:generate` · تفريغ المفتاح من `.env.example`
- [ ] حذف `backup.sql` و`backup_clean.sql` من المستودع
- [ ] إصلاح مسارات checkout المكررة ومسار `thankyou` المفقود
- [ ] تغيير كلمات مرور الحسابات الافتراضية أو تعطيل إنشائها في الإنتاج

### مهم — قبل استقبال عملاء حقيقيين
- [ ] إكمال Stripe (مبلغ فعلي + Webhook + تسجيل الدفعات) أو تعطيله مؤقتاً والاكتفاء بـ COD
- [ ] إصلاح خطأي `getConfigValue` والتشفير في إعدادات البوابات
- [ ] تفعيل الشحن (إلغاء تعليق المسارات + حساب التكلفة في الـ checkout)
- [ ] بريد تأكيد الطلب + إشعار الأدمن/البائع بالطلب الجديد (مع `QUEUE_CONNECTION=database` وworker)
- [ ] Throttling للدخول + `session()->regenerate()` للبائع + فحص حالة البائع
- [ ] إصلاح حذف الطلبات عبر البائعين (§3.1-9)
- [ ] `lockForUpdate()` عند خصم المخزون
- [ ] إصلاح أو حذف `apiResource('brands')`
- [ ] إزالة سجلات الـ Debug من `SiteSettingsController` و`theme.php`
- [ ] `SESSION_SECURE_COOKIE=true` وتفعيل تشفير الجلسة

### تحسينات — بعد الإطلاق
- [ ] اختبارات للـ checkout والسلة والكوبونات وعزل البائعين
- [ ] CI (GitHub Actions): lint + tests + build
- [ ] توحيد كود التعامل مع الملفات (3 أنماط متضاربة تسبب ملفات يتيمة — `BannerService` و`ProfileController`s)
- [ ] ترجمة `category.php` الناقصة للعربية (11 سطراً)
- [ ] صفحة تسجيل بائع + شاشة تعديل بائع للأدمن
- [ ] تفعيل الاستردادات فعلياً

---

## 5. خطة الانتقال إلى Supabase (PostgreSQL) 🐘

### 5.1 نقطة مهمة أولاً: ما الذي يستضيفه Supabase؟

Supabase يستضيف **قاعدة البيانات (PostgreSQL)** فقط — تطبيق Laravel نفسه يحتاج استضافة PHP منفصلة (VPS / Railway / Fly.io / Laravel Forge + DigitalOcean / استضافة مشتركة تدعم PHP 8.2). يمكن لاحقاً استخدام **Supabase Storage** (متوافق S3) لتخزين صور المنتجات بدل القرص المحلي — خطوة اختيارية موصى بها.

### 5.2 الخبر الجيد: الكود متوافق إلى حد كبير

الفحص الشامل وجد أن الانتقال **أنظف من المعتاد**:
- ✅ لا FULLTEXT ولا دوال MySQL خاصة (لا `GROUP_CONCAT` ولا `DATE_FORMAT`...) ولا Stored Procedures ولا Triggers
- ✅ 7 مواضع SQL خام فقط في كامل المشروع، محصورة في ملفين
- ✅ اتصال `pgsql` معرّف مسبقاً في `config/database.php`
- ✅ كل الـ Transactions عبر `DB::transaction()` القياسي (محمول)
- ✅ الكاش والجلسات والطوابير على `file`/`sync` — لا تلمس قاعدة البيانات

### 5.3 العوائق التي يجب إصلاحها (بالترتيب)

| الأولوية | العائق | الملف | الحل | الجهد |
|---|---|---|---|---|
| ✅ | امتداد PHP `pdo_pgsql` — **مُفعّل** (PHP 8.5.5، السطران 934 و936) | `C:\php\php.ini` | تم — `PDO::getAvailableDrivers()` يعرض `pgsql` | تم |
| 🔴 | `SET FOREIGN_KEY_CHECKS` — أمر MySQL فقط | `IbnTaymiyyahBookstoreSeeder.php:42,61` | احذف السطرين (ترتيب الحذف صحيح أصلاً) | 5 دقائق |
| 🔴 | `->change()` و`renameColumn()` تحتاجان `doctrine/dbal` وغير مثبتة | migrations `2025_12_31_185724` و`2026_01_05_135955` | `composer require doctrine/dbal` | 15 دقيقة |
| 🔴 | نقل البيانات — ملفا الدمب بصيغة MariaDB لا يقبلهما PostgreSQL | `backup*.sql` | شغّل `php artisan migrate` على Supabase لبناء المخطط، ثم انقل **البيانات فقط** عبر pgloader أو CSV، ثم اضبط الـ sequences (انظر 5.4) | 4–8 ساعات |
| 🟠 | استعلام فرز السعر سيفشل على PG — `groupBy` + `orderByRaw(MIN(...))` مع `withCount`/`withAvg` | `Store/ShopController.php:90-99` | أعد كتابته بـ subquery: `orderBy(ProductVariant::select('price')->whereColumn('product_id','products.id')->orderBy('price')->limit(1))` | 2–3 ساعات |
| 🟠 | `LIKE` حساس لحالة الأحرف على PG — البحث الإنجليزي سيتأثر (العربية لا تتأثر) | `SearchController.php:19,52` · `SubscriberController.php:20` · `MenuItemRepository.php:31` | استبدل `'like'` بـ `'ilike'`، وفعّل `case_insensitive` في إعدادات yajra DataTables | 1–2 ساعة |
| 🟡 | 14 عمود `enum()` في 13 جدولاً | جداول: orders، payments، refunds، vendors، customers، coupons، brands... | تعمل على PG كـ `varchar + CHECK` تلقائياً، لكن يُنصح بتحويلها إلى `string` + تحقق في التطبيق لسهولة التعديل لاحقاً | 3–4 ساعات (اختياري) |
| 🟡 | عمود `SKU` بأحرف كبيرة → يصبح معرّفاً حساساً `"SKU"` على PG | `create_product_variants_table.php:21` | أعد تسميته `sku` وحدّث الموديل والواجهات | 1 ساعة |
| 🟢 | 5 أعمدة `json` | payments، refunds، shipping_zones | تُبنى أصلياً عند الـ migrate؛ يفضَّل `jsonb` | 30 دقيقة |
| 🟢 | جدول `password_resets` القديم بلا مفتاح أساسي (مكرر مع `password_reset_tokens`) | migration `2014_10_12` | احذف الجدول القديم | 15 دقيقة |
| 🟢 | خصم المخزون بلا قفل — أخطر على PG (عزل `READ COMMITTED`) | الـ checkout | أضف `->lockForUpdate()` عند قراءة مخزون المتغير | 1–2 ساعة |

### 5.4 خطوات التنفيذ العملية

**الخطوة 1 — إنشاء مشروع Supabase:**
من [supabase.com](https://supabase.com) → New Project → احفظ كلمة مرور القاعدة. من Settings → Database ستجد بيانات الاتصال.

**الخطوة 2 — إعداد الاتصال في `.env`:**
```env
DB_CONNECTION=pgsql
# استخدم Session Pooler (يدعم IPv4) — وليس Transaction Pooler
DB_HOST=aws-0-<region>.pooler.supabase.com
DB_PORT=5432
DB_DATABASE=postgres
DB_USERNAME=postgres.<project-ref>
DB_PASSWORD=<كلمة المرور>
```
وفي `config/database.php` غيّر `'sslmode' => 'prefer'` إلى `'require'`.

> ⚠️ **تحذيران مهمان:**
> - استخدم منفذ **5432 (Session Mode)** لأوامر `migrate` والتطبيق. منفذ **6543 (Transaction Mode)** يكسر Prepared Statements في Laravel ما لم تفعّل `PDO::ATTR_EMULATE_PREPARES`.
> - الاتصال المباشر `db.<ref>.supabase.co` يعمل عبر IPv6 فقط في الخطة المجانية — استخدم عنوان الـ Pooler أعلاه.

**الخطوة 3 — إصلاحات الكود** (حسب جدول 5.3): حذف سطري `FOREIGN_KEY_CHECKS`، تثبيت `doctrine/dbal`، إصلاح `ShopController`، تحويل `like` → `ilike`.

**الخطوة 4 — بناء المخطط:**
```bash
php artisan migrate --force
```

**الخطوة 5 — نقل البيانات** (البيانات الحالية تجريبية في معظمها — قد يكون الأسهل إعادة الـ seeding بدل النقل):
- **الخيار أ (أنظف):** `php artisan db:seed` + `php artisan matchstore:install --with-import` لإعادة توليد البيانات على Supabase مباشرة، ثم تشغيل `IbnTaymiyyahBookstoreSeeder` (بعد إصلاحه).
- **الخيار ب (نقل فعلي):** pgloader من MariaDB إلى PostgreSQL، أو تصدير كل جدول CSV واستيراده بـ `\copy`، **ثم إعادة ضبط العدّادات** لكل الجداول (وإلا ستتصادم المعرّفات الجديدة):
```sql
SELECT setval(pg_get_serial_sequence('products','id'), COALESCE(MAX(id),1)) FROM products;
-- كرر لكل جدول (54 جدولاً)
```

**الخطوة 6 — الاختبار الشامل:** البحث، فلاتر المتجر (خاصة الفرز بالسعر والتقييم)، الـ checkout بالكامل، جداول DataTables في لوحة الأدمن، تبديل اللغة والعملة.

**الخطوة 7 (اختياري لاحقاً) —** نقل صور المنتجات إلى Supabase Storage عبر واجهته المتوافقة مع S3 (`composer require league/flysystem-aws-s3-v3` + إعداد disk `s3` بمفاتيح Supabase Storage).

> **ملاحظة عن Row Level Security:** ستظهر تنبيهات في لوحة Supabase بأن الجداول بلا RLS. هذا طبيعي — Laravel يتصل بدور `postgres` متجاوزاً RLS، والأمان في طبقة التطبيق. **لكن لا تفعّل** Supabase Auto APIs (PostgREST) على هذه الجداول للعموم.

### 5.5 حالة التنفيذ — 2026-08-28 ✅

| البند | الحالة |
|---|---|
| مشروع Supabase | ✅ `sdnkvokzbslziodmwhoa` — منطقة `eu-central-1` — PostgreSQL 17.6 |
| `.env` | ✅ `DB_CONNECTION=pgsql` + Session Pooler (5432) + `DB_SSLMODE=require` |
| امتداد `pdo_pgsql` | ✅ مُفعّل في `C:\php\php.ini` |
| بناء المخطط | ✅ `php artisan migrate --force` — **65 migration نُفّذت، 0 معلّقة** (لم تحتج `doctrine/dbal`) |
| البيانات | ✅ مُعبّأة: لغات (ar/en)، عملات (USD/JOD/NIS)، إعدادات الموقع، القائمة، الصفحات (3×2 لغة)، 3 مناطق شحن + 27 محافظة، بوابات الدفع (PayPal/Stripe **معطّلتان** لأن مفاتيحهما placeholders — الدفع عند الاستلام فقط)، **مكتبة ابن تيمية**: 8 أقسام، 5 ناشرين، 22 كتاباً، مستخدم أدمن `admin@example.com` (دور admin) |
| Supabase MCP | ✅ `.mcp.json` + Agent Skills (`.agents/skills`) — المصادقة عبر `claude /mcp` |
| إصلاحات الكود (5.3) | ✅ كانت منفَّذة سابقاً (`ShopController` subquery، `ilike` عبر ماكرو، حذف `FOREIGN_KEY_CHECKS`، `lockForUpdate`). متبقٍّ اختياري: عمود `"SKU"` الحساس لحالة الأحرف، وأعمدة `json` → `jsonb` |
| أداء الصفحات | ✅ أُصلح N+1 حرج: `StoreMenuComposer`/`AdminLanguageComposer` كانا يستعلمان لكل partial (186 استعلاماً للرئيسية → **21**، المتجر 129 → **23**) عبر singletons تحفظ النتيجة لكل طلب + eager loading لـ `images`/`thumbnail`/`primaryVariant`. محلياً الصفحات 8–17 ث بسبب كمون ~500ms/استعلام إلى eu-central-1؛ على خادم في نفس المنطقة تصبح أجزاء الثانية |
| `composer install` | ⚠️ حزم `nette/*` المقفولة تشترط PHP ≤ 8.4؛ محلياً (PHP 8.5.5) استخدم `composer install --ignore-platform-req=php`. للإنتاج استخدم PHP 8.2–8.4 |
| TLS | ⚠️ Supabase لا يفرض SSL من جهته (اتصال `sslmode=disable` يُقبل) — العميل محمي بـ `require`؛ يُنصح بتفعيل **Enforce SSL** من Dashboard → Settings → Database |

> **ملاحظة Windows:** روابط `.claude/skills/*` هي git symlinks؛ بدون Developer Mode تُستخرج كملفات نصية. الحل المحلي: `New-Item -ItemType Junction` من `.claude/skills/<name>` إلى `.agents/skills/<name>` (ثم `git update-index --skip-worktree` عليها).

---

## 6. خارطة الطريق المقترحة

| المرحلة | المحتوى | المدة التقديرية |
|---|---|---|
| **1. الإصلاحات الأمنية الحرجة** | البنود الحرجة في §4 (المسارات المكشوفة، الصلاحيات، SVG، XSS، المفاتيح) | 2–3 أيام |
| **2. الانتقال إلى Supabase** | §5 كاملاً + الاختبار الشامل | 2–3 أيام |
| **3. إكمال مسار الشراء** | الشحن، إصلاح checkout، Stripe/PayPal الفعلي أو COD فقط مبدئياً، قفل المخزون | 3–5 أيام |
| **4. البريد والإشعارات** | تأكيد الطلب، إشعارات الأدمن/البائع، SMTP حقيقي، طابور + worker | 1–2 يوم |
| **5. التجهيز للنشر** | استضافة PHP، `.env` إنتاجي، حذف `hot`، symlink، HTTPS، نسخ احتياطي | 1–2 يوم |
| **6. اختبار وإطلاق تجريبي** | اختبارات يدوية شاملة + اختبارات آلية أساسية للـ checkout | 2–3 أيام |

**الإجمالي: ~11–18 يوم عمل.**

**قرار يُنصح به لتسريع الإطلاق:** أطلق النسخة الأولى بـ **الدفع عند الاستلام فقط** (المسار الوحيد المكتمل والمختبَر) وأجّل بوابات الدفع الإلكتروني لإصدار لاحق — هذا يوفّر 3–4 أيام ويقلّص المخاطر المالية، ويناسب سوق الأردن/فلسطين المستهدف.

---

## 7. ملاحظات إضافية

- **جودة عامة جيدة:** لا يوجد `dd()` أو `TODO` منسية في الكود، والموديلات منظمة، والترجمة عبر جداول ترجمة نظيفة.
- **ازدواجية كود:** `Vendor/ProductController` نسخة شبه مطابقة من `Admin/ProductController` (374 سطراً)، و`ImageService` مكرر — فرصة توحيد لاحقاً.
- **ملفات ميتة:** `ShippingZoneController` كامل لكن مساراته معلّقة · `StoreSettingController` بلا مسار · migration فارغ `2026_04_17_add_status_to_promo_cards`.
- **الموديل `Vendor` بلا علاقات** رغم وجود `vendor_id` في المنتجات — كل الاستعلامات يدوية.
- **احرص على commits صغيرة من الآن فصاعداً** — الـ commit الوحيد الحالي لا يوفر أي نقطة رجوع.
