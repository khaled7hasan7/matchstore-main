# تقرير حالة المشروع — MatchStore (مكتبة ابن تيمية)

**تاريخ الفحص:** 2026-08-27
**آخر تحديث:** 2026-08-27 — ✅ **أُنجزت جميع الإصلاحات الأمنية الحرجة** (§3.1 كاملاً) و✅ **المرحلة 3: مسار الشراء** (§8) و✅ **المرحلة 2: جاهزية Supabase/PostgreSQL** مُتحققة على PostgreSQL 16 حقيقي (§9) و✅ **المرحلة 4: البريد والإشعارات** — تأكيد الطلب للعميل + إشعارات الأدمن والبائعين + نموذج التواصل، كلها عبر الطابور وثنائية اللغة (§10). المتبقي على Supabase خطوات إعداد فقط (§5.4).
**الخلاصة:** المشروع **غير جاهز للإطلاق حالياً**. البنية الأساسية قوية ومكتملة (كتالوج، سلة، حسابات، لوحات تحكم)، لكن توجد **ثغرات أمنية حرجة**، ومسار الدفع الإلكتروني غير مكتمل (الدفع عند الاستلام فقط يعمل)، ولا توجد رسائل بريد للطلبات، ولا يوجد أي إعداد للنشر. الانتقال إلى Supabase **ممكن وجهده معقول** (التفاصيل في القسم 5).

**تقدير الجهد الإجمالي للوصول للإطلاق: 2–3 أسابيع عمل مركّز.**

---

## 1. نظرة عامة على المشروع

| البند | التفاصيل |
|---|---|
| النوع | منصة تجارة إلكترونية متعددة البائعين (Multi-Vendor) — نسخة معدّلة من مشروع Velstore مفتوح المصدر |
| الهوية الحالية | يجري تحويله إلى **مكتبة ابن تيمية** (متجر كتب عربي، عملة JOD، شحن للأردن وفلسطين) عبر `IbnTaymiyyahBookstoreSeeder` |
| التقنيات | Laravel 10 · PHP 8.1+ · MariaDB 10.11 (حالياً) · Bootstrap 5 · Vite 5 |
| الحجم | 55 جدول · 47 موديل · 69 متحكم (Controller) · ~200 مسار (Route) · 124 ملف Blade |
| اللغات | عربي + إنجليزي — الترجمة العربية مكتملة ~99.5% مع دعم RTL كامل |
| اللوحات | أدمن (`/admin`) · بائع (`/seller`) · متجر العملاء (`/`) + واجهة API للقراءة |
| بوابات الدفع | الدفع عند الاستلام (يعمل) · PayPal (شبه مكتمل) · Stripe (تجريبي غير صالح) |
| سجل Git | **commit واحد فقط** (`v1`) — لا يوجد تاريخ تطوير أو نقطة رجوع |

---

## 2. ما هو جاهز ويعمل ✅

- **إدارة الكتالوج كاملة:** منتجات + متغيرات (variants) + خصائص (attributes) + صور + تصنيفات + علامات تجارية + بانرات + قوائم + صفحات CMS + بطاقات ترويجية — كلها بنظام ترجمة متعدد اللغات.
- **لوحة البائع:** إدارة منتجات وطلبات ومراجعات وملف شخصي.
- **تجربة التسوق:** سلة (Session) + كوبونات + مفضلة (Wishlist) + بحث + فلاتر + مراجعات + عرض سريع.
- **حسابات العملاء:** تسجيل/دخول/استعادة كلمة مرور + عناوين متعددة + سجل طلبات (3 أنظمة دخول منفصلة: أدمن/بائع/عميل).
- **الدفع عند الاستلام (COD):** المسار الوحيد المكتمل من السلة حتى تأكيد الطلب.
- **بيانات الشحن:** محافظات الأردن وفلسطين مع التكاليف (Seeder جاهز).
- **الواجهة:** ثيم xylo متجاوب Mobile-First، وملفات الإنتاج مبنية (`public/build` موجود).
- **نقاط قوة في الكود:** لا يوجد أي موديل بـ `$guarded = []` (حماية Mass Assignment سليمة) · حماية CSRF بدون استثناءات · كلمات المرور مشفرة bcrypt · لا توجد مفاتيح API مسربة في الكود.

---

## 3. المشاكل الحرجة — يجب إصلاحها قبل أي نشر 🔴

### 3.1 ثغرات أمنية

| # | الثغرة | الموقع | الأثر |
|---|---|---|---|
| 1 | **مسارات إعدادات الموقع بدون حماية** — أربعة مسارات `site-settings` (قراءة وتعديل ورفع ملفات وتغيير الثيم) خارج مجموعة `auth` | `routes/web.php:185-188` | أي زائر مجهول يستطيع تغيير اسم الموقع والشعار والثيم ورفع ملفات — تشويه كامل للموقع |
| 2 | **رفع SVG بدون حماية → XSS مخزّن** — الشعار والأيقونة يقبلان SVG (يمكن أن يحمل `<script>`) عبر المسار غير المحمي أعلاه | `SiteSettingsController.php:40-41` و `BrandController.php:44,76` | تنفيذ سكربت على كل زائر بما فيهم الأدمن |
| 3 | **لا يوجد نظام صلاحيات** — لوحة الأدمن محمية بـ `auth` فقط؛ جدول `users` بلا عمود دور/صلاحية، ولا توجد Policies | `routes/web.php:55` · `app/Models/User.php` | أي حساب في جدول users = أدمن كامل (طلبات، عملاء، مفاتيح بوابات الدفع) |
| 4 | **مراجعات بدون تسجيل دخول ومعتمدة تلقائياً** (`is_approved => 1`) | `routes/store.php:56` · `Store/ReviewController.php:27-33` | سبام مراجعات غير محدود على أي منتج |
| 5 | **XSS عبر وصف المنتج** — `{!! $product->translation->description !!}` يعرض HTML البائع بدون تعقيم، ولا يوجد HTML Purifier في المشروع | `themes/xylo/product-detail.blade.php:1893,2221` | بائع خبيث يحقن سكربت في صفحات المنتجات |
| 6 | **لا يوجد حد لمحاولات الدخول** (Throttling) لدخول العميل والبائع | `Store/Auth/LoginController.php` · `Vendor/Auth/AuthController.php` | هجمات تخمين كلمات المرور |
| 7 | **Session Fixation** — دخول البائع لا يستدعي `session()->regenerate()` | `Vendor/Auth/AuthController.php:25-27` | اختطاف جلسات |
| 8 | **حالة البائع غير مفعّلة** — البائع الموقوف/المحظور يدخل لوحته بشكل طبيعي | `Middleware/AuthenticateVendor.php` | لا يمكن إيقاف بائع فعلياً |
| 9 | **بائع يحذف طلبات بائعين آخرين** — يكفي أن يملك منتجاً واحداً في الطلب ليحذف الطلب كاملاً | `Vendor/OrderController.php:59-64` | تدمير سجلات بائعين آخرين وسجل العميل |
| 10 | **مفتاح تشفير حقيقي منشور** في `.env.example` (سطر 3) | `.env.example:3` | كل من ينسخه يحصل على مفتاح معروف → تزوير الجلسات. يجب تفريغه وتوليد مفتاح جديد للإنتاج |
| 11 | **نسختا قاعدة بيانات مرفوعتان في المستودع** (`backup.sql` و`backup_clean.sql`) | جذر المشروع | قد تحويان بيانات شخصية/هاشات — يجب حذفهما من Git |
| 12 | **حسابات افتراضية ضعيفة** — المثبّت ينشئ `admin@example.com` / `abc123` | `Console/Commands/MatchStore.php:88-92` | حساب أدمن معروف للجميع إن شُغّل المثبّت على الإنتاج |
| 13 | **تسجيل Debug يفضح البيانات** — `Log::info('All Request Data:', $request->all())` يكتب كامل الطلبات في السجل | `SiteSettingsController.php:102-189` | تسريب بيانات + تضخم سجلات |

### 3.2 وظائف معطوبة أو ناقصة

| # | المشكلة | الموقع | التفاصيل |
|---|---|---|---|
| 1 | **مسار `thankyou` غير موجود** — `checkout.store` يعيد التوجيه لمسار غير معرّف → استثناء | `CheckoutController.php:225` | يوجد مساران متكرران للـ checkout (`store.php:47-48` و `web.php:190-191`) والأقدم منهما معطوب — يجب حذف المسار القديم |
| 2 | **Stripe غير صالح للاستخدام** — مبلغ ثابت 1000 سنت (10$) بغضّ النظر عن السلة، بيئة sandbox ثابتة، لا Webhook، لا يُسجَّل طلب أو دفعة | `Store/PaymentGateway/StripeController.php:14-17` · `StripeService.php` | إعادة بناء كاملة مطلوبة (PaymentIntent بالمبلغ الفعلي + Webhook + تسجيل Payment/Order) |
| 3 | **PayPal بلا وضع Live** — `'sandbox'` ثابتة في 3 مواضع، ولا تحقق من صحة `?token` عند الرجوع | `CheckoutController.php:116,142` · `StripeController.php:13` | يعمل تجريبياً فقط |
| 4 | **خطأ جلب مفاتيح البوابات** — `getConfigValue($key, $default)` يستقبل البيئة كوسيط ثانٍ لكنه لا يفلتر عليها؛ مع وجود مفاتيح sandbox وlive معاً قد تُستخدم **مفاتيح Live وهو يظن أنه sandbox** | `Models/PaymentGateway.php:24` | خطير مالياً |
| 5 | **خطأ تشفير إعدادات البوابات** — القيمة تُشفَّر عند الحفظ ولا تُفكّ عند القراءة → تُرسَل مفاتيح مشفرة (خربانة) لـ Stripe/PayPal | `Models/PaymentGatewayConfig.php:17-20` | عملياً يجبر التخزين بنص صريح |
| 6 | **الشحن نصف معطّل** — مسارات إدارة مناطق الشحن معلّقة بتعليق، وصفحة الدفع تثبّت `$shipping = null` | `routes/web.php:150-153` · `CheckoutController.php:43` | لا تُحتسب تكلفة شحن رغم جاهزية الجداول والبيانات |
| 7 | **لا يوجد أي بريد للطلبات** — لا تأكيد طلب، لا إشعار بائع، لا إيصال دفع، لا رد على نموذج التواصل (يوجد Mailable واحد فقط للنشرة البريدية) | `app/Mail/` | أساسي لأي متجر |
| 8 | **الاستردادات (Refunds) للعرض فقط** — لا يوجد إجراء موافقة/تنفيذ | `Admin/RefundController.php` | |
| 9 | **API معطوب** — `apiResource('brands')` مسجّل لكن المتحكم يحوي `index` فقط → المسارات الأخرى ترجع 500 | `routes/api.php:39` | |
| 10 | **خطر بيع فوق المخزون** — خصم المخزون داخل Transaction لكن بدون `lockForUpdate()` — يزداد سوءاً على PostgreSQL | `CheckoutController` | |
| 11 | **البائع لا يسجّل نفسه** — لا صفحة تسجيل بائع؛ الأدمن ينشئ البائعين فقط (ولا يمكنه تعديلهم — لا يوجد edit/update) | `Admin/VendorController.php` | قرار منتج: هل هذا مقصود؟ |
| 12 | **لا توجد صور منتجات على القرص** — مجلد `storage/app/public/products` غير موجود أصلاً | `storage/app/public/` | البيانات التجريبية بلا صور |

### 3.3 مشاكل بيئة النشر

| # | المشكلة | الأثر |
|---|---|---|
| 1 | **ملف `public/hot` موجود** — يجعل Laravel يطلب الأصول من خادم التطوير `[::1]:5173` بدل `public/build` | الموقع سيظهر **بلا تصميم ولا جافاسكربت** على أي خادم — احذفه قبل النشر |
| 2 | **رابط `public/storage` معطوب** — يشير لمسار XAMPP القديم `C:\xampp\htdocs\...` | كل الصور المرفوعة لن تظهر — أعد تنفيذ `php artisan storage:link` |
| 3 | **`.env` بإعدادات تطوير بالكامل** — `APP_DEBUG=true` (يفضح كل شيء عند أي خطأ)، `QUEUE_CONNECTION=sync`، بريد `mailpit`، قاعدة بيانات `root` بلا كلمة مرور | يجب ملف `.env` إنتاجي كامل |
| 4 | **صفر اختبارات فعلية** — ملفا Laravel الافتراضيان فقط | لا شبكة أمان لأي تعديل |
| 5 | **لا CI ولا Docker ولا سكربت نشر** — النشر يدوي بالكامل | مع النقطتين 1 و2 أعلاه، النشر اليدوي سيفشل غالباً |
| 6 | **الاعتماد على قاعدة البيانات في كل طلب** — `LocaleMiddleware` يستعلم `site_settings` في كل صفحة؛ إن سقطت القاعدة يسقط الموقع كاملاً بخطأ 500 | أضف معالجة فشل (fallback) |

---

## 4. قائمة التحقق قبل الإطلاق (مرتبة بالأولوية)

### حرج — لا نشر قبلها
- [x] ~~نقل مسارات `site-settings` داخل مجموعة `auth`~~ (§3.1-1) — **تم:** المسارات الأربعة الآن تحت `/admin` خلف `auth` + `admin`
- [x] ~~إزالة `svg` من قوائم `mimes:` في كل نقاط الرفع~~ (§3.1-2) — **تم:** الشعار والأيقونة والعلامات التجارية والبانرات
- [x] ~~إضافة عمود دور للمستخدمين + Middleware `admin` لكل لوحة الأدمن~~ (§3.1-3) — **تم:** migration لعمود `role` + `EnsureUserIsAdmin` + تعطيل التسجيل العام في جدول users
- [x] ~~حماية المراجعات بـ `auth.customer` وإيقاف الاعتماد التلقائي~~ (§3.1-4) — **تم:** + حد 10 محاولات/دقيقة
- [x] ~~تعقيم وصف المنتج (HTMLPurifier)~~ (§3.1-5) — **تم:** حزمة `ezyang/htmlpurifier` + صنف `App\Support\HtmlSanitizer` (مُختبر: يزيل script/onerror/javascript:)
- [ ] حذف `public/hot` وإعادة إنشاء `storage` symlink (§3.3-1,2) — **ملاحظة:** الملفان غير موجودين في المستودع أصلاً؛ هذه خطوة تُنفّذ على جهازك المحلي/الخادم فقط: احذف `public/hot` ثم `php artisan storage:link`
- [ ] `.env` إنتاجي: `APP_DEBUG=false` · `APP_ENV=production` · مفتاح جديد `php artisan key:generate` — **تم جزئياً:** فُرّغ المفتاح المسرّب من `.env.example`؛ يتبقى إعداد `.env` الإنتاجي عند النشر
- [x] ~~حذف `backup.sql` و`backup_clean.sql` من المستودع~~ — **تم** + إضافة `/*.sql` إلى `.gitignore`. ⚠️ ما زالا في تاريخ Git القديم — أي كلمات مرور بداخلهما تُعتبر مكشوفة ويجب تغييرها
- [x] ~~إصلاح مسارات checkout المكررة ومسار `thankyou` المفقود~~ — **تم:** حُذف المسار القديم المعطوب والدالة الميتة؛ بقي مسار `checkout.process` العامل
- [x] ~~تغيير كلمات مرور الحسابات الافتراضية~~ — **تم:** المثبّت يولّد كلمات مرور عشوائية (أدمن + بائع تجريبي) ويعرضها مرة واحدة في الطرفية

### مهم — قبل استقبال عملاء حقيقيين
- [ ] إكمال Stripe (مبلغ فعلي + Webhook + تسجيل الدفعات) أو تعطيله مؤقتاً والاكتفاء بـ COD — **تم جزئياً:** صفحة الدفع الآن لا تُحمّل سكربتات Stripe/PayPal إلا إذا كانت البوابة مفعّلة وبمفاتيح حقيقية (كان مفتاح `Stripe("asdasd")` الوهمي قادراً على كسر الصفحة)؛ COD يعمل بشكل مستقل تماماً. لتفعيل COD فقط: عطّل Stripe وPayPal من لوحة الأدمن → بوابات الدفع
- [x] ~~إصلاح خطأي `getConfigValue` والتشفير في إعدادات البوابات~~ — **تم:** فلترة حسب البيئة (sandbox/live) + فك التشفير عند القراءة مع تحمّل القيم القديمة غير المشفرة — مُختبر بـ 7 تحققات
- [x] ~~تفعيل الشحن (حساب التكلفة في الـ checkout)~~ — **تم:** الطلب يحسب تكلفة الشحن من المنطقة المختارة **في الخادم** (27 منطقة أردنية وفلسطينية جاهزة عبر `shipping_regions` + API) ويخزّنها في الطلب مع تفصيل الإجماليات. ملاحظة: مسارات `ShippingZoneController` المعلقة تُدير جدول zones قديماً غير مستخدم — تُركت معلقة عمداً والآلية الفعلية هي regions
- [x] ~~بريد تأكيد الطلب + إشعار الأدمن/البائع بالطلب الجديد~~ — **تم:** 4 رسائل بريدية (تأكيد للعميل + إشعار للأدمن + إشعار لكل بائع بمنتجاته فقط + رسائل نموذج التواصل) كلها ShouldQueue وثنائية اللغة مع RTL. فعّل في الإنتاج: `QUEUE_CONNECTION=database` + `php artisan queue:work --tries=3`
- [x] ~~Throttling للدخول + `session()->regenerate()` للبائع + فحص حالة البائع~~ — **تم:** حد المحاولات لدخول العميل والبائع والتسجيل واستعادة كلمة المرور، تجديد جلسة البائع عند الدخول وإبطالها عند الخروج، والبائع الموقوف/المحظور يُطرد فوراً من أي طلب
- [x] ~~إصلاح حذف الطلبات عبر البائعين~~ (§3.1-9) — **تم:** الحذف مسموح فقط إذا كانت كل عناصر الطلب من منتجات البائع نفسه
- [x] ~~`lockForUpdate()` عند خصم المخزون~~ — **تم:** (الحقيقة أن خصم المخزون لم يكن موجوداً أصلاً!) الآن يُحجز المخزون داخل Transaction مع قفل الصفوف، ويُرفض الطلب إذا نفدت الكمية، ويُعاد المخزون عند إلغاء الطلب
- [ ] إصلاح أو حذف `apiResource('brands')`
- [x] ~~إزالة سجلات الـ Debug من `SiteSettingsController` و`theme.php`~~ — **تم** + إزالة تسجيل كامل بيانات المنتج في `ProductRepository` (أدمن وبائع)
- [ ] `SESSION_SECURE_COOKIE=true` وتفعيل تشفير الجلسة

### تحسينات — بعد الإطلاق
- [x] ~~اختبارات للـ checkout والسلة والكوبونات~~ — **تم:** حزمة `CheckoutTest` (7 اختبارات، 45 تحققاً) تغطي مسار الشراء كاملاً + smoke test، على SQLite في الذاكرة (`php artisan test`)
- [ ] CI (GitHub Actions): lint + tests + build
- [ ] توحيد كود التعامل مع الملفات (3 أنماط متضاربة تسبب ملفات يتيمة — `BannerService` و`ProfileController`s)
- [ ] ترجمة `category.php` الناقصة للعربية (11 سطراً)
- [ ] صفحة تسجيل بائع + شاشة تعديل بائع للأدمن
- [ ] تفعيل الاستردادات فعلياً

---

## 5. خطة الانتقال إلى Supabase (PostgreSQL) 🐘

### 5.1 نقطة مهمة أولاً: ما الذي يستضيفه Supabase؟

Supabase يستضيف **قاعدة البيانات (PostgreSQL)** فقط — تطبيق Laravel نفسه يحتاج استضافة PHP منفصلة (VPS / Railway / Fly.io / Laravel Forge + DigitalOcean / استضافة مشتركة تدعم PHP 8.2). يمكن لاحقاً استخدام **Supabase Storage** (متوافق S3) لتخزين صور المنتجات بدل القرص المحلي — خطوة اختيارية موصى بها.

### 5.2 الخبر الجيد: الكود متوافق إلى حد كبير

الفحص الشامل وجد أن الانتقال **أنظف من المعتاد**:
- ✅ لا FULLTEXT ولا دوال MySQL خاصة (لا `GROUP_CONCAT` ولا `DATE_FORMAT`...) ولا Stored Procedures ولا Triggers
- ✅ 7 مواضع SQL خام فقط في كامل المشروع، محصورة في ملفين
- ✅ اتصال `pgsql` معرّف مسبقاً في `config/database.php`
- ✅ كل الـ Transactions عبر `DB::transaction()` القياسي (محمول)
- ✅ الكاش والجلسات والطوابير على `file`/`sync` — لا تلمس قاعدة البيانات

### 5.3 العوائق التي يجب إصلاحها (بالترتيب)

| الأولوية | العائق | الملف | الحل | الجهد |
|---|---|---|---|---|
| 🔴 | امتداد PHP غير مفعّل — `pdo_pgsql` معطّل في XAMPP | `C:\xampp\php\php.ini` سطرا 947 و949 | أزل `;` من `;extension=pdo_pgsql` و`;extension=pgsql` وأعد تشغيل Apache (الملفات موجودة في `ext/`) | 5 دقائق |
| ✅ | ~~`SET FOREIGN_KEY_CHECKS` — أمر MySQL فقط~~ — **تم** حذفهما، والسيدر أصبح أيضاً مكتفياً ذاتياً (ينشئ البائع والمتجر بنفسه) وidempotent — مُشغَّل مرتين بنجاح على PostgreSQL | — | — | — |
| ✅ | ~~`->change()` و`renameColumn()` تحتاجان `doctrine/dbal` وغير مثبتة~~ — **تم:** `doctrine/dbal` مثبتة الآن، وأُصلح migration `2026_01_05_135955` (كان يجمع `renameColumn` مع إضافة أعمدة في كتلة واحدة — يعمل على MySQL فقط ويفسد الجدول على SQLite/PostgreSQL) | — | — | — |
| ✅ | ~~نقل البيانات~~ — **الخيار أ (إعادة الزرع) أصبح مثبتاً عملياً:** migrate + كل السيدرز + سيدر المكتبة نفّذت كاملة على PostgreSQL 16 (راجع الأوامر الجاهزة في §5.4). لا حاجة لـ pgloader إلا إذا أردت نقل طلبات/عملاء حقيقيين من MariaDB | — | — | — |
| ✅ | ~~استعلام فرز السعر سيفشل على PG~~ — **تم** بإعادة كتابته بـ subquery ارتباطي، **واكتُشف أثناءها 4 أخطاء إضافية أُصلحت:** فلتر التقييم يولّد `SELECT *` مع `GROUP BY` (مرفوض على PG) + ربط نصّي يُفشل الفلتر صامتاً على SQLite + فرز `name_asc` على عمود غير موجود أصلاً + فلتر المخزون على عمود باسم خاطئ (`stock_quantity`) | — | — | — |
| ✅ | ~~`LIKE` حساس لحالة الأحرف على PG~~ — **تم:** أُضيف macro محمول `whereLike`/`orWhereLike` (يستخدم `ILIKE` على pgsql فقط) وطُبّق على بحث المنتجات والمشتركين وslugs القوائم؛ إعداد yajra `case_insensitive` كان مفعّلاً أصلاً. مُختبر: البحث بـ "SAHIH" الكبيرة يطابق على PG | — | — | — |
| ✅ | ~~14 عمود `enum()`~~ — **مُتحقق:** تُبنى تلقائياً كـ `varchar + CHECK` على PG وتعمل (كل الـ migrations والاختبارات مرّت). التحويل إلى `string` يبقى تحسيناً اختيارياً لاحقاً | — | — | — |
| ✅ | ~~عمود `SKU` بأحرف كبيرة~~ — **مُتحقق: لا حاجة لإعادة التسمية.** كل الوصول عبر Query Builder الذي يقتبس المعرّف `"SKU"` تلقائياً — الاختبارات تُدخل وتستعلم SKU على PG بنجاح. أعد التسمية فقط إن كتبت SQL خام مستقبلاً | — | — | — |
| ✅ | ~~5 أعمدة `json`~~ — **مُتحقق:** بُنيت أصلياً عند migrate على PG. الترقية إلى `jsonb` تحسين اختياري | — | — | — |
| ✅ | ~~جدول `password_resets` القديم (مكرر)~~ — **تصحيح مهم للتقرير: الجدول ليس مكرراً — إنه جدول استعادة كلمة مرور العملاء** (`config/auth.php` broker `customers`) وحذفه كان سيكسر الاستعادة! **تم** بدلاً من ذلك: migration جديد يضيف مفتاحاً أساسياً على `email` (يزيل تحذير Supabase ويسمح بالـ replication) | — | — | — |
| ✅ | ~~خصم المخزون بلا قفل — أخطر على PG~~ — **تم** ضمن المرحلة 3: حجز المخزون بـ `lockForUpdate()` داخل Transaction | — | — | — |

### 5.4 خطوات التنفيذ العملية

**الخطوة 1 — إنشاء مشروع Supabase:**
من [supabase.com](https://supabase.com) → New Project → احفظ كلمة مرور القاعدة. من Settings → Database ستجد بيانات الاتصال.

**الخطوة 2 — إعداد الاتصال في `.env`:**
```env
DB_CONNECTION=pgsql
# استخدم Session Pooler (يدعم IPv4) — وليس Transaction Pooler
DB_HOST=aws-0-<region>.pooler.supabase.com
DB_PORT=5432
DB_DATABASE=postgres
DB_USERNAME=postgres.<project-ref>
DB_PASSWORD=<كلمة المرور>
```
وفي `config/database.php` غيّر `'sslmode' => 'prefer'` إلى `'require'`.

> ⚠️ **تحذيران مهمان:**
> - استخدم منفذ **5432 (Session Mode)** لأوامر `migrate` والتطبيق. منفذ **6543 (Transaction Mode)** يكسر Prepared Statements في Laravel ما لم تفعّل `PDO::ATTR_EMULATE_PREPARES`.
> - الاتصال المباشر `db.<ref>.supabase.co` يعمل عبر IPv6 فقط في الخطة المجانية — استخدم عنوان الـ Pooler أعلاه.

**الخطوة 3 — إصلاحات الكود: ✅ منجزة بالكامل ومدفوعة للمستودع** (حذف `FOREIGN_KEY_CHECKS`، تثبيت `doctrine/dbal`، إعادة كتابة استعلامات `ShopController`، macro `whereLike` المحمول، إصلاح migrations الجمع بين rename والإضافة).

**الخطوة 4 — بناء المخطط والبيانات** (التسلسل الكامل المُختبر فعلياً على PostgreSQL 16):
```bash
php artisan migrate --force
php artisan db:seed --force --class=LanguageSeeder
php artisan db:seed --force --class=CurrencySeeder
php artisan db:seed --force --class=ShippingRegionSeeder
php artisan db:seed --force --class=PaymentGatewaySeeder
php artisan db:seed --force --class=PaymentGatewayConfigSeeder
php artisan db:seed --force --class=MenuSeeder
php artisan db:seed --force --class=PageSeeder
php artisan db:seed --force --class=SiteSettingsSeeder
php artisan db:seed --force --class=IbnTaymiyyahBookstoreSeeder
```
ثم أنشئ حساب الأدمن: `php artisan install:matchstore` (يولّد كلمة مرور عشوائية ويعرضها مرة واحدة)، أو أنشئه يدوياً.

> السيدرز الآن idempotent — إعادة تشغيلها آمنة. **الخيار ب (نقل فعلي من MariaDB)** يلزم فقط إن كانت لديك طلبات/عملاء حقيقيون: pgloader أو CSV عبر `\copy`، ثم إعادة ضبط العدّادات: `SELECT setval(pg_get_serial_sequence('products','id'), COALESCE(MAX(id),1)) FROM products;` لكل جدول.

**الخطوة 5 — الاختبار:** الحزمة الآلية تغطي الأساس وقد **نجحت كاملة على PostgreSQL** (15 اختباراً: checkout كاملاً، الفرز والفلاتر والبحث). شغّلها بنفسك على قاعدة Supabase فارغة إن أردت:
```bash
DB_CONNECTION=pgsql DB_HOST=... DB_DATABASE=... DB_USERNAME=... DB_PASSWORD=... php artisan test
```
يتبقى يدوياً: جداول DataTables في لوحة الأدمن، وتبديل اللغة/العملة في المتصفح.

**الخطوة 7 (اختياري لاحقاً) —** نقل صور المنتجات إلى Supabase Storage عبر واجهته المتوافقة مع S3 (`composer require league/flysystem-aws-s3-v3` + إعداد disk `s3` بمفاتيح Supabase Storage).

> **ملاحظة عن Row Level Security:** ستظهر تنبيهات في لوحة Supabase بأن الجداول بلا RLS. هذا طبيعي — Laravel يتصل بدور `postgres` متجاوزاً RLS، والأمان في طبقة التطبيق. **لكن لا تفعّل** Supabase Auto APIs (PostgREST) على هذه الجداول للعموم.

---

## 6. خارطة الطريق المقترحة

| المرحلة | المحتوى | المدة التقديرية |
|---|---|---|
| **1. الإصلاحات الأمنية الحرجة** ✅ | ~~البنود الحرجة في §4~~ — **أُنجزت** (راجع §7) | ~~2–3 أيام~~ |
| **2. الانتقال إلى Supabase** ✅ (شق الكود) | ~~§5 كاملاً~~ — **الكود جاهز ومُتحقق منه على PostgreSQL 16 حقيقي** (migrations + seeders + كامل الاختبارات — راجع §9). المتبقي خطوات إعداد على حسابك: إنشاء مشروع Supabase + ملء `.env` + تنفيذ أوامر §5.4 | ~2–3 ساعات إعداد |
| **3. إكمال مسار الشراء** ✅ | ~~الشحن، إصلاح checkout، COD، قفل المخزون~~ — **أُنجزت بنهج COD أولاً** (راجع §8) | ~~3–5 أيام~~ |
| **4. البريد والإشعارات** ✅ | ~~تأكيد الطلب، إشعارات الأدمن/البائع، طابور~~ — **أُنجزت** (راجع §10). يتبقى عند النشر: بيانات SMTP حقيقية في `.env` + تشغيل worker | ~ساعة إعداد |
| **5. التجهيز للنشر** | استضافة PHP، `.env` إنتاجي، حذف `hot`، symlink، HTTPS، نسخ احتياطي | 1–2 يوم |
| **6. اختبار وإطلاق تجريبي** | اختبارات يدوية شاملة + اختبارات آلية أساسية للـ checkout | 2–3 أيام |

**الإجمالي: ~11–18 يوم عمل.**

**قرار يُنصح به لتسريع الإطلاق:** أطلق النسخة الأولى بـ **الدفع عند الاستلام فقط** (المسار الوحيد المكتمل والمختبَر) وأجّل بوابات الدفع الإلكتروني لإصدار لاحق — هذا يوفّر 3–4 أيام ويقلّص المخاطر المالية، ويناسب سوق الأردن/فلسطين المستهدف.

---

## 7. سجل الإصلاحات المنفذة (2026-08-27)

عشرة commits على الفرع `claude/convert-file-to-markdown-6fl8fu`، وتغطي الثغرات الـ13 كلها في §3.1:

| Commit | المحتوى |
|---|---|
| نظام الأدوار | عمود `role` في users (القدامى يُرحّلون كأدمن) + `EnsureUserIsAdmin` على كامل `/admin` + نقل مسارات site-settings خلف الحماية + **تعطيل التسجيل العام** (كان أي زائر يسجّل حساباً في جدول users ويصبح أدمن!) + المثبّت يولّد كلمة مرور عشوائية |
| منع SVG | إزالة svg من 7 قواعد رفع (شعار، أيقونة، علامات تجارية، بانرات) |
| المراجعات | `auth.customer` + throttle + `is_approved = 0` حتى موافقة الأدمن |
| تعقيم الوصف | HTMLPurifier على وصف المنتج في الموضعين (سطح المكتب والجوال) — مُختبر ضد script/onerror/javascript: |
| تأمين الدخول | Throttling (عميل/بائع/تسجيل/استعادة) + تجديد جلسة البائع + فحص حالة البائع في كل طلب |
| حذف الطلبات | البائع يحذف فقط الطلبات المكونة من منتجاته حصراً (403 لغير ذلك) |
| تنظيف المستودع | حذف نسختي قاعدة البيانات + تفريغ APP_KEY المسرّب من `.env.example` + `.gitignore` |
| سجلات Debug | إزالة كل `Log::info` الكاشفة للبيانات + كلمة مرور عشوائية للبائع التجريبي |
| checkout | حذف المسارين المكررين والدالة الميتة الموجهة لـ `thankyou` غير الموجود |
| توافق Linux | إصلاح `use App\Services\vendor\...` (حرف صغير) — كان سيكسر لوحة البائع كلياً على أي خادم Linux |

**تحقق تم إجراؤه:** `composer install` سليم، و`php artisan route:list` يحلّ الـ261 مساراً بنجاح، والـ middleware مؤكدة على المسارات الحساسة (site-settings خلف auth+admin، المراجعات خلف auth.customer+throttle، دخول البائع خلف throttle).

> **مطلوب منك بعد سحب التغييرات:** `composer install` ثم `php artisan migrate` (migration الأدوار الجديد)، وبما أن كلمة مرور الأدمن القديمة `abc123` معروفة — غيّرها فوراً أو أعد تشغيل المثبّت.

---

## 8. سجل إصلاحات المرحلة 3 — مسار الشراء (2026-08-27)

نُفّذت بنهج **COD أولاً** كما أوصى التقرير. خمسة commits إضافية:

| Commit | المحتوى |
|---|---|
| مسار الشراء | الطلب يخزّن الآن `subtotal` + `shipping_cost` + `discount` + `coupon_code` (migration جديد)؛ تكلفة الشحن تُحسب في الخادم من المنطقة المختارة (27 منطقة جاهزة)؛ الكوبون يُعاد التحقق منه لحظة الطلب ويُطبَّق خصمه فعلياً على الإجمالي (كان يُتجاهل تماماً!)؛ حجز المخزون بـ `lockForUpdate()` داخل Transaction مع رفض الطلب عند نفاد الكمية؛ السلة الفارغة تُرفض؛ صفحة الدفع تعرض صف الخصم وتحمي COD من سكربتات بوابات غير مهيأة؛ صفحة التأكيد تعرض تفصيل الإجماليات ولم تعد تسرّب طلبات الضيوف لأي زائر |
| إلغاء الطلبات | **اكتشاف:** لوحة الأدمن كانت ترسل `cancelled` وقاعدة البيانات تقبل `canceled` فقط — أي أن إلغاء أي طلب كان مستحيلاً. أُصلح التعارض، والإلغاء الآن يعيد المخزون المحجوز (مرة واحدة فقط) |
| بوابات الدفع | `getConfigValue` تفلتر بالبيئة (كانت قد تخلط مفاتيح live مع sandbox — خطر مالي)؛ القيم المشفرة تُفك عند القراءة (كانت تُرسل مشفرة للبوابات) مع تحمّل الصفوف القديمة |
| الاختبارات | `CheckoutTest` جديدة: 7 اختبارات (45 تحققاً) تغطي المسار كاملاً — إنشاء طلب COD بالإجماليات الصحيحة، التراجع عند نفاد المخزون، رفض الكوبون المنتهي والمنطقة الموقوفة، خصوصية صفحة التأكيد، إلغاء الأدمن مع إعادة المخزون ومنع غير الأدمن، ورفض السلة الفارغة |
| محمولية القاعدة | `doctrine/dbal` مثبتة + فصل `renameColumn` عن إضافة الأعمدة في migration الطلبات (كان يفسد الجدول على SQLite/PostgreSQL — أساسي لخطة Supabase) |

**التحقق:** كامل الحزمة أخضر — **9 اختبارات، 48 تحققاً** (`php artisan test` على SQLite في الذاكرة) + `route:list` سليم + كل القوالب تُجمَّع بنجاح.

**ملاحظتان للنشر:**
- حساب الشحن حالياً بوزن ثابت (1 كغم = التكلفة الأساسية للمنطقة) في الواجهة والخادم معاً بشكل متسق — ربط الوزن الفعلي للكتب خطوة لاحقة سهلة (`per_kg_cost` جاهز في الجداول).
- للإطلاق بـ COD فقط: عطّل PayPal وStripe من لوحة الأدمن → بوابات الدفع (زر التفعيل)، وستختفي خياراتهما وسكربتاتهما من صفحة الدفع تلقائياً.

---

## 9. سجل إصلاحات المرحلة 2 — جاهزية Supabase/PostgreSQL (2026-08-27)

**منهج التحقق:** بدل الاكتفاء بالإصلاح النظري، شُغّل PostgreSQL 16 فعلي وأُعيد عليه كل شيء: **كل الـ60 migration نجحت، وكل السيدرز نجحت (وسيدر المكتبة مرتين لإثبات الـ idempotency)، وكامل حزمة الاختبارات (15 اختباراً / 64 تحققاً) خضراء على PostgreSQL وSQLite معاً**، مع فحوص يدوية على بيانات المكتبة المزروعة (22 كتاباً): فرز السعر تصاعدياً/تنازلياً، الفرز بالاسم المترجم، البحث الإنجليزي بحالة أحرف مختلفة (ILIKE)، والبحث العربي.

أبرز ما اكتُشف وأُصلح أثناء التحقق (لم يكن في التقرير الأصلي):

| الاكتشاف | التفصيل |
|---|---|
| فلتر التقييم مكسور بصمت | يولّد `SELECT *` مع `GROUP BY` (يرفضه PG بخطأ صريح)، وقيمة التقييم كانت تُربط نصياً فيفشل الفلتر **بصمت** على SQLite — أعيدت كتابته بـ subquery ارتباطي وربط رقمي |
| فرز الاسم معطوب على كل القواعد | `orderBy('name')` وجدول المنتجات لا يحوي عمود name أصلاً (الاسم في الترجمات) — كان يرمي استثناء على MariaDB أيضاً |
| فلتر المخزون معطوب على كل القواعد | يستعلم عن عمود `stock_quantity` غير الموجود (الصحيح `stock`) |
| سيدر المكتبة يفترض بيانات ديمو | كان يعتمد على وجود بائع/متجر مسبقاً (`?? 1`) فيفشل على قاعدة نظيفة — أصبح ينشئهما بنفسه، ويمرر قيمة enum صحيحة لحالة العلامة التجارية (كان يمرر boolean يقبله MySQL فقط) |
| `CurrencySeeder` يتصادم مع migrations البيانات | العملات تُدخل أصلاً في migrations — أصبح idempotent بـ `updateOrInsert` |
| تصحيح توصية خاطئة في التقرير | جدول `password_resets` ليس مكرراً بل هو جدول استعادة كلمة مرور **العملاء** — حذفه كان سيكسرها؛ أُضيف له مفتاح أساسي بدلاً من ذلك |
| تسلسلات PG كشفت افتراضاً هشاً | `shops.vendor_id` الافتراضي (1) ينكسر مع تسلسلات PostgreSQL — الاختبارات تعيّنه صراحة الآن |

**ما يلزمك لإتمام الانتقال فعلياً** (خطوات إعداد فقط، الكود جاهز):
1. أنشئ مشروع Supabase واحفظ كلمة مرور القاعدة (§5.4 خطوة 1)
2. املأ `.env` ببيانات الـ Session Pooler + `DB_SSLMODE=require` — القالب جاهز معلّقاً في `.env.example`
3. نفّذ أوامر §5.4 خطوة 4 (migrate + seeders بالترتيب المذكور)
4. على جهازك المحلي (XAMPP) لو أردت التطوير على PG محلياً: فعّل `extension=pdo_pgsql` و`extension=pgsql` في `php.ini`

---

## 10. سجل إصلاحات المرحلة 4 — البريد والإشعارات (2026-08-27)

كان المشروع بلا أي بريد للطلبات إطلاقاً (Mailable وحيد للنشرة البريدية). أُضيف:

| الرسالة | إلى | المحتوى |
|---|---|---|
| `OrderConfirmationMail` | العميل | تأكيد الطلب: المنتجات، تفصيل الإجماليات (فرعي/خصم/شحن/إجمالي)، عنوان الشحن، مدة التوصيل المتوقعة من المنطقة، وتنويه الدفع عند الاستلام |
| `NewOrderAdminMail` | بريد الموقع (`contact_email` من إعدادات الموقع) | إشعار بطلب جديد مع بيانات العميل والطلب كاملاً |
| `VendorOrderMail` | كل بائع في الطلب | **منتجات ذلك البائع فقط** مع إجماليها — البائع لا يرى منتجات غيره أبداً |
| `ContactMessageMail` | بريد الموقع | رسائل نموذج التواصل مع `Reply-To` لبريد المرسل (الرد المباشر يصل صاحب الرسالة) + حد محاولات على النموذج |

**خصائص التنفيذ:**
- كل الرسائل **ShouldQueue** — لا تعطّل صفحة الدفع؛ وثنائية اللغة (عربي RTL/إنجليزي) وتُرسل بلغة تصفح العميل وقت الطلب.
- **البريد لا يكسر الطلب أبداً:** كل إرسال داخل try/catch مع تسجيل الفشل فقط — يوجد اختبار يوجّه البريد لخادم SMTP وهمي غير موجود ويتأكد أن الطلب يُنشأ ويُوجَّه العميل للتأكيد بشكل طبيعي.
- اسم المتجر ورمز العملة يُلتقطان لحظة الطلب (عامل الطابور بلا جلسة).
- قوالب البريد HTML بسيطة بأنماط inline (متوافقة مع عملاء البريد) وأجزاء مشتركة (عناصر/إجماليات/عنوان).

**الاختبارات:** 5 اختبارات بريد جديدة (إرسال الثلاثية للطلب، عرض القوالب الثلاثة بالمحتوى الصحيح، سلامة الطلب عند فشل SMTP، تخزين وإرسال رسالة التواصل مع Reply-To، وعرض قالبها) — **الحزمة الكاملة الآن 20 اختباراً / 88 تحققاً، خضراء على SQLite وPostgreSQL معاً**.

**مطلوب عند النشر (موثق في `.env.example`):**
```env
QUEUE_CONNECTION=database
MAIL_MAILER=smtp
MAIL_HOST=<مزود SMTP: Brevo/Mailgun/Postmark/SES>
MAIL_PORT=587
MAIL_USERNAME=... MAIL_PASSWORD=... MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="orders@yourdomain.com"
```
مع تشغيل worker دائم: `php artisan queue:work --tries=3` (عبر Supervisor أو systemd)، وتأكد أن `contact_email` مضبوط في إعدادات الموقع من لوحة الأدمن.

---

## 11. ملاحظات إضافية

- **جودة عامة جيدة:** لا يوجد `dd()` أو `TODO` منسية في الكود، والموديلات منظمة، والترجمة عبر جداول ترجمة نظيفة.
- **ازدواجية كود:** `Vendor/ProductController` نسخة شبه مطابقة من `Admin/ProductController` (374 سطراً)، و`ImageService` مكرر — فرصة توحيد لاحقاً.
- **ملفات ميتة:** `ShippingZoneController` كامل لكن مساراته معلّقة · `StoreSettingController` بلا مسار · migration فارغ `2026_04_17_add_status_to_promo_cards`.
- **الموديل `Vendor` بلا علاقات** رغم وجود `vendor_id` في المنتجات — كل الاستعلامات يدوية.
- **احرص على commits صغيرة من الآن فصاعداً** — الـ commit الوحيد الحالي لا يوفر أي نقطة رجوع.
